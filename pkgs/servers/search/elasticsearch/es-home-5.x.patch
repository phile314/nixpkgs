diff -rupN a/bin/elasticsearch b/bin/elasticsearch
--- a/bin/elasticsearch	2017-02-22 14:06:00.492557145 +0100
+++ b/bin/elasticsearch	2017-02-22 14:07:07.178814552 +0100
@@ -105,7 +105,10 @@ while [ -h "$SCRIPT" ] ; do
 done
 
 # determine elasticsearch home
-ES_HOME=`dirname "$SCRIPT"`/..
+if [ -z "$ES_HOME" ]; then
+    echo "You must set the ES_HOME var" >&2
+    exit 1
+fi
 
 # make ELASTICSEARCH_HOME absolute
 ES_HOME=`cd "$ES_HOME"; pwd`
diff -rupN a/bin/elasticsearch-plugin b/bin/elasticsearch-plugin
--- a/bin/elasticsearch-plugin	2017-02-22 14:10:30.087683250 +0100
+++ b/bin/elasticsearch-plugin	2017-02-22 14:06:16.975115099 +0100
@@ -16,7 +16,10 @@ while [ -h "$SCRIPT" ] ; do
 done
 
 # determine elasticsearch home
-ES_HOME=`dirname "$SCRIPT"`/..
+if [ -z "$ES_HOME" ]; then
+  echo "You must set the ES_HOME var" >&2
+  exit 1
+fi
 
 # make ELASTICSEARCH_HOME absolute
 ES_HOME=`cd "$ES_HOME"; pwd`
